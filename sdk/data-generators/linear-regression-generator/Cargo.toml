[package]
authors = ["Shale Xiong <shale.xiong@arm.com>"]
description = "A utility for generating synthetic datasets for use with the `linear-regression` example."
edition = "2018"
name = "linear-regression-generator"
version = "0.3.0"
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = "2.33"
postcard = { version = "0.7.2", features = [ "alloc" ] }
rand = "0.7"
rand_distr = "0.3"
