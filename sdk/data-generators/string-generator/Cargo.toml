[package]
authors = ["The Veracruz Development Team"]
description = "A utility for generating synthetic strings for use with various examples (e.g. `string-edit-distance`)."
edition = "2018"
name = "string-generator"
version = "0.3.0"
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bitflags = "=1.2.1"
clap = "2.33"
pinecone = { git = "https://github.com/egrimley-arm/pinecone.git", branch = "veracruz" }
# Required for std::error::Error from pinecone
serde = { version = "1.0.103", features = ["derive"] }
